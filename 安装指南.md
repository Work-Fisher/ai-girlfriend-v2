# AI 女友 · Windows 一键包 · 安装指南

这份文档讲得比「使用说明.txt」细，装的时候卡住了再翻。顺利的话你只需要那份。

---

## 一、这玩意儿是什么

一个跑在你自己电脑上的语音陪伴程序。你说话，她听懂、回答、用你指定的嗓音念出来，
同时生成对口型的视频画面。

| 部件 | 在哪跑 | 说明 |
|---|---|---|
| 语音识别 | 本地 GPU | Whisper large-v3-turbo |
| 语音合成 | 本地 GPU | OmniVoice，声音从你传的参考录音克隆 |
| 口型生成 | 本地 GPU（WSL 里） | Duix/HeyGem 256v1 |
| 语言模型 | **外部 API** | 智谱 GLM / Kimi / DeepSeek，你自己填 Key |

只有"她说什么"这一步要联网，其余全在本地。

> 从 GitHub 下载的源码不包含大模型、WSL 镜像、固定 runtime 和依赖层，源码本身不是完整一键包。各模型官方地址、固定版本、目录位置和原作者参考资料见 [`docs/MODELS.md`](docs/MODELS.md)。下面的双击流程面向文件齐全的 Windows 整合包。

**为什么大模型走外部 API 而不是本地跑**：本地跑一个像样的对话模型至少要 8G 显存，
和语音、口型抢显卡。走 API 之后整机 12G 显存就够，回答质量还更好。代价是要自己
申请一个 Key，按用量付费（日常聊天一个月通常几块钱）。

---

## 二、装之前

### 机器

- **Windows 10 / 11 64 位**
- **NVIDIA 显卡**，驱动别太旧
  实测 RTX 4070 Ti（12G 显存）：五个服务全开常驻约 7G，生成口型时更高。
  8G 显存能用，但建议关掉数字人。**没有 N 卡跑不起来。**
- **CPU 虚拟化已开启**
  任务管理器 → 性能 → CPU，右下角看「虚拟化」。显示「已禁用」就要进 BIOS 开
  （Intel 叫 VT-x / Virtualization Technology，AMD 叫 SVM Mode，各家主板名字不一样）。
- **磁盘留 35 GB 以上**
  包本身 14.5 GB，导入数字人引擎时还要再展开约 11 GB。之后生成的音视频继续占。

### WSL2

数字人引擎跑在 WSL2 里。你不需要先手工安装：双击「首次安装.cmd」后，安装器会
请求管理员权限并自动检查、启用 WSL2。

如果 Windows 以前从未启用过 WSL，第一次只会完成 Windows 组件安装，随后必须重启。
重启后再双击一次「首次安装.cmd」，它会从中断处继续。Windows 安装或更新 WSL 组件时
可能需要联网；数字人引擎镜像本身已经在整合包里，不会重复下载。

> 不想折腾 WSL？跳过就是了。不装的话没有口型画面，纯语音对话照常用，
> 而且更省显存、响应更快。直接跳到「四、日常使用」。

### 放在哪儿

**必须是纯英文、无空格的路径。**

```
✓  D:\AI-Girlfriend
✓  E:\tools\ai-girlfriend
✗  D:\AI女友                  数字人引擎在 WSL 里，中文路径它看不见
✗  D:\My Projects\AI          空格会让部分脚本参数解析出错
```

还有：**要完整解压**，不能在压缩软件里双击运行。

---

## 三、安装

### 第 1 步：首次安装

双击 **「首次安装.cmd」**。它做四件事：

```
1/4  检查整合包完整性     缺哪个模型会直接报名字
2/4  检查 WSL2           没有就请求管理员授权并自动启用
3/4  导入数字人引擎        wsl --import --version 2，5.6 GB 解压成约 11 GB，几分钟
4/4  自检并记录安装状态
```

全绿就成了。中间报错的话，它会连着「该怎么办」一起打出来，照着做。

> 导入数字人镜像不联网。只有 Windows 尚未安装 WSL 组件时，系统安装 WSL 可能联网，
> 并且会要求重启一次。

如果只想先体验纯语音，可以完全跳过首次安装，直接双击「一键启动.cmd」。启动器会自动
进入纯语音模式，不会再卡在命令行里询问。

### 第 2 步：第一次启动

双击 **「一键启动.cmd」**。五个服务按顺序起来，浏览器自动打开 `127.0.0.1:7860`。

第一次启动慢是正常的：语音模型要加载进显存，数字人引擎冷启动要一到三分钟。
之后就快了。

### 第 3 步：界面里的设置

打开界面，右上角「设置」。最低限度只需配置语言模型；包里已有默认声音和演示角色：

**① 语言模型**

点「＋ 添加提供方」，选一家，填 Key：

| 提供方 | 去哪申请 | 备注 |
|---|---|---|
| 智谱 GLM | bigmodel.cn | 有 Flash 免费档 |
| Kimi | platform.moonshot.cn | |
| DeepSeek | platform.deepseek.com | 便宜 |

Base URL 会自动填好，模型从下拉里选。也可以选「自定义提供方」接别家，
只要是 OpenAI 兼容的接口就行。

> Key 明文存在 `app\heygem-data\llm-provider.json`，别把这个文件发给别人。

**② 角色影像**

传一段视频，她的脸和口型都从这儿来。

- 5~15 秒
- 正脸，少遮挡（头发别盖住嘴、别戴口罩）
- 光线亮一点 —— **这条最影响成片清晰度**，实测源素材越亮越清楚
- 顺便传一段「待机影像」：她不说话时循环播放的，挑一段嘴部安静的，切换才不突兀

**③ 声音**

传一段参考录音 + 这段录音的**逐字文本**。

- 3~10 秒最好（超过 20 秒会被自动截断）
- **一句完整的话**，不要跨句剪
- 干净人声，没有背景音乐和混响
- 逐字文本必须和录音一字不差，标点也要对 —— 这个直接决定像不像

包里自带一个「苏晚-单句」的音色可以先试，参考录音是 AI 合成的，能随便用。

---

## 四、日常使用

以后每次就双击「一键启动.cmd」。

界面左下角有两个开关：

- **数字人** —— 关掉就是纯语音对话，快很多、省显存
- **免提** —— 麦克风常开，说完自动断句，不用按住

中间那个大圆圈是麦克风，按住说话（免提模式下点一下开始/暂停）。

### 命令行参数

在 PowerShell 里跑可以带参数：

```powershell
.\一键启动.cmd -SkipDigitalHuman    # 这次不启动数字人
.\一键启动.cmd -NoBrowser           # 不自动开浏览器
```

---

## 五、她的记忆

换模型、改人设、重启电脑，她都记得之前聊过什么。这不是把聊天记录塞回提示词，
是真的把上一段对话接着往下聊 —— 记录存在本地 `app\dsh-bridge\dsh-home\sessions\`。

界面左边那条「AFFINITY」进度条是好感度。聊得越多、处得越久涨得越高，
分成六档，每上一档她说话的亲近程度会变。计分只认真实发生过的对话和
真正来过的日子，挂机不涨分，单日也有上限 —— 想涨快只能隔天再来。

**想让她彻底忘掉重新开始**：删掉 `app\dsh-bridge\dsh-home\sessions` 整个文件夹。

---

## 六、卡住了

### 「首次安装.cmd」提示重启

这是第一次启用 WSL2 的正常步骤。重启 Windows 后，再双击一次「首次安装.cmd」。

### 「首次安装.cmd」报 WSL 相关的错

先确认弹出的 Windows 管理员授权窗口点了「是」，再确认 BIOS 里虚拟化已开启。
仍然失败时，在管理员 PowerShell 里运行 `wsl --status` 查看系统给出的原因。

### 导入数字人引擎失败

- 磁盘空间够不够（要 20 GB 以上余量）
- `wsl --status` 看 WSL 本身正不正常
- 同名发行版已存在：`.\首次安装.cmd -Force` 会先注销再导入

### 启动时说端口被占用

某个端口被别的软件占了，报错会说是哪个。关掉那个软件，或者查一下：

```powershell
Get-NetTCPConnection -State Listen -LocalPort 7860
```

### 有声音，但画面不动

数字人引擎没起来。先关掉程序再打开一次 —— 引擎冷启动要一到三分钟，
第一次慢是正常的。还不行就看 `app\logs\duix.stdout.log`。

### 说了话没反应

1. 浏览器有没有给麦克风权限（地址栏左边的小图标）
2. 右上角显示「已在新窗口连接」= 开了多个标签页，关掉多余的
3. 一直转圈不说话 → 设置 → 服务，看「语言模型」那行是不是绿的。
   多半是 Key 填错或者余额没了。

### 口型糊、不清楚

源视频的问题，不是引擎的问题。实测下来**源素材越亮、嘴部动作越清楚，
成片越清晰**。换一段光线好的正脸视频，比调任何参数都管用。

### 显存不够

关掉「数字人」开关。纯语音对话占用小得多。

### 双击 cmd 闪一下就关

报错来不及看。在文件夹空白处按住 **Shift + 右键** →「在此处打开 PowerShell 窗口」，
输入 `.\一键启动.cmd` 回车，错误信息就留在屏幕上了。

---

## 七、目录里都是什么

```
AI女友/
├── 首次安装.cmd          装一次就行
├── 一键启动.cmd          以后每次点这个
├── 使用说明.txt
├── 安装指南.md           你正在看的
├── runtime/              Python + ffmpeg
├── AI-Girlfriend-Models/ 语音识别、语音合成、断句模型
├── engine/
│   └── duix-rootfs.tar.gz    数字人引擎镜像，装完可以删（5.6 GB）
└── app/                  程序 + 你的数据
    ├── heygem-data/      角色视频、好感度、设置、API Key
    ├── voices/           音色
    ├── config/           人设
    ├── dsh-bridge/       语言模型桥接 + 聊天记录
    ├── output/           生成的音视频
    └── logs/             日志
```

**备份的话，`app\` 整个存一份就够了。** 换机器时把 `app\heygem-data`、`app\voices`、
`app\config`、`app\dsh-bridge\dsh-home\sessions` 拷过去，人设、音色、好感度和记忆都在。

`engine\duix-rootfs.tar.gz` 装完之后就没用了，想省 5.6 GB 可以删 —— 但重装时要重新下。

---

## 八、卸载

1. 删掉整个文件夹
2. 数字人引擎在 WSL 里，单独删：

```powershell
wsl --unregister DuixDistro
```

---

## 九、隐私

- 对话内容会发给你在设置里选的那家 API 提供方。这是唯一出网的数据。
- 语音、录音、视频、聊天记录全部只存在本地，不上传任何地方。
- API Key 明文存在 `app\heygem-data\llm-provider.json`。分享文件夹前记得删掉。
- 如果你要把自己的角色视频或音色分发给别人：那是真人的肖像和声音，
  拿别人的之前先问过本人。
